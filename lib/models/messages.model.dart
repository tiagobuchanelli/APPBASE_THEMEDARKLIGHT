class MessagestModel {
  String idMessage;
  String user;
  String date;
  String lastMessage;
  String imageUser;
  int totalMessages;
  String idUser;

  MessagestModel(this.idMessage, this.user, this.date, this.lastMessage,
      this.imageUser, this.totalMessages, this.idUser);
}
